export declare class NgxHighlightJsThemeService {
    private _defaultTheme;
    private _jsURL;
    _theme: string;
    _initializedPromise: Promise<void>;
    _linkEl: HTMLLinkElement;
    constructor(_defaultTheme: string, _jsURL: string);
    initialized(): Promise<void>;
    loadTheme(name: string): Promise<void>;
    readonly theme: string;
}
